<template id="linkn-tabgroup">
  <ul class="tabs">
    <linkn-tab v-for="(tab, index) in tabs"
                 :key="index"
                 v-bind="tab"
                 :event="event"
                 :class="{ 'is-active': selected === index }">
  </ul>
</template>
